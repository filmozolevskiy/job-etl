# Airflow core dependencies
apache-airflow[postgres,redis]==2.8.1

# Database connectors
psycopg2-binary==2.9.9
# SQLAlchemy version must be <2.0 for Airflow 2.8.1 compatibility
# Already included in Airflow dependencies

# dbt dependencies
dbt-core==1.7.4
dbt-postgres==1.7.4

# Tableau Hyper API for publisher
tableauhyperapi

# HTTP requests (already included in Airflow)
# requests==2.31.0
# urllib3==2.1.0

# Data processing (already included in Airflow)
# pandas==2.1.4
# numpy==1.26.2

# JSON handling (already included in Airflow)
# jsonschema==4.20.0

# Environment and configuration (already included in Airflow)
# python-dotenv==1.0.0

# Logging and monitoring
structlog==23.2.0

# Testing (for development)
pytest==7.4.3
pytest-cov==4.1.0

# Code quality
flake8==6.1.0
black==23.11.0
isort==5.12.0

# Security (use version compatible with Airflow providers)
# cryptography>=42.0.0 - conflicts with gcloud-aio-auth, msal, snowflake-connector-python
# Using version compatible with Airflow providers
cryptography<42.0.0,>=41.0.5

# Utilities (already included in Airflow)
# click==8.1.7
# python-dateutil==2.8.2
# pytz==2023.3
